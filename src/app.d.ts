declare namespace App {
	interface Locals {
		loggedIn: boolean
		userId: string | null
		profileId: string | null
		username: string | null
		role: string | null
	}

	interface PageData {}

	interface Platform {}
}
